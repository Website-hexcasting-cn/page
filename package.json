{
  "name": "",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/fetch-hexdoc-patterns.mjs --if-missing && astro build",
    "preview": "astro preview",
    "fetch:patterns": "node scripts/fetch-hexdoc-patterns.mjs",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.2.0",
    "pako": "^3.0.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^7.0.2"
  }
}